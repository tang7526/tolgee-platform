import type { UiProps, UiInterface, UiKeyOption } from '@tolgee/core';
export declare class UI implements UiInterface {
    private props;
    private viewerComponent;
    private keyContextMenu;
    constructor(props: UiProps);
    renderViewer(key: string, defaultValue: string | undefined, ns: string[]): void;
    getKey(props: {
        keys: Map<string, string | undefined>;
        openEvent: MouseEvent;
    }): Promise<string | undefined>;
    handleElementClick(keysAndDefaults: UiKeyOption[], event: MouseEvent): Promise<void>;
}
