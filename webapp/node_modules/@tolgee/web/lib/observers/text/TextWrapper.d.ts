import type { TranslatePropsInternal, WrapperMiddleware } from '@tolgee/core';
declare type Props = {
    inputPrefix: string;
    inputSuffix: string;
    translate: (params: TranslatePropsInternal) => string;
};
export declare const TextWrapper: ({ inputPrefix, inputSuffix, translate, }: Props) => WrapperMiddleware;
export {};
