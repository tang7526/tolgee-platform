import type { DefaultParamType, TolgeeInstance } from '@tolgee/web';
import React from 'react';
export declare type ParamsTags = DefaultParamType | ((value: any) => JSX.Element | React.ReactElement | null) | React.ReactNode;
export declare type ReactOptions = {
    useSuspense: boolean;
};
export declare type TolgeeReactContext = {
    tolgee: TolgeeInstance;
    options: ReactOptions;
};
